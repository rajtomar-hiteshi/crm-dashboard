# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/leadgen_crm

# SMTP settings for password reset emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
